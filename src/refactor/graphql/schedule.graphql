query GetSchedule($filter: ScheduleFilter!) {
  schedule(filter: $filter) {
    items {
      ...Program
    }
  }
}

fragment Program on ScheduleItem {
  startsAt
  endsAt
  video {
    id
    title
    organization {
      id
      name
    }
  }
}
