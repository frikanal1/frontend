fragment FrontpageSchedule on ScheduleItem {
  startsAt
  endsAt
  video {
    id
    title
    description
    organization {
      id
      name
    }
  }
}

query GetFrontpage {
  schedule(filter: {}) {
    items {
      ...FrontpageSchedule
    }
  }
}
