

fragment FrontpageSchedule on ScheduleItem {
   startsAt
    video {
        id
        title
        description
        organization {
            id
            name
        }
    }
}

query GetFrontpage {
    schedule (filter: {}) {
        items {
            ...FrontpageSchedule
        }
    }
}